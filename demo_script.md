# AIMV 演示解说脚本
> 比赛 Demo 讲解用 · 视频时长 4:57 · 建议语速：中速匀速

---

## 开场（视频开始前，播放前说）

> 大家好。我们要演示的是 **AIMV** —— 一个让任何人都能用对话创作专业级音乐视频的 AI 平台。
> 整个流程从一句话开始，到一部完整的 MV 结束，全程无需任何视频剪辑技能。

---

## `00:00` 首页 · 平台亮相

**画面**：AIMV 首页加载，Hero 区展示平台 Slogan，向下滚动逐步呈现功能模块。

> 这是 AIMV 的首页。平台核心价值只有一句话：**用对话替代剪辑**。
>
> 向下滚动可以看到三大核心能力：
> - **AI 导演**：基于 CrewAI 多 Agent 框架，自动完成从创意到分镜的全部规划
> - **多模型编排**：按场景自动调度 Veo 3.1 / Seedance / Suno 等最新模型
> - **可视化节点图**：每一帧素材都可溯源，随时进入 Canvas 精细调整

---

## `00:28` 登录

**画面**：跳转登录页，输入 demo / demo123，跳转到项目列表。

> 我们用演示账号登录。每位用户拥有独立的项目空间，所有创作数据持久保存。

---

## `00:37` 项目列表

**画面**：展示多个项目卡片，鼠标悬停触发 hover 动效，显示缩略图和状态标签。

> 这是项目中心。每张卡片对应一个独立的 MV 项目，可以看到它的封面预览和生成状态。
> 今天我们要从零开始，创作一部全新的 MV。

---

## `00:44` 进入空白 Studio

**画面**：点击 DEMO 项目，进入 Studio 界面——左侧聊天区空白，右侧状态面板等待中。

> 这是 Studio —— AIMV 的核心创作空间。
> 左侧是与 AI 导演对话的区域，右侧实时跟踪生成进度。
> 此刻什么都没有，只有一个空白的开始。

---

## `00:50` 第一轮对话 · 定主题

**画面**：用户在输入框输入：
> *"帮我做一部暗黑觉醒风格的MV，主角是短发少女对抗扑克牌社会，赛博电影感"*

> 我们描述一个创意方向：
> **暗黑觉醒 × 反乌托邦**——主角 EIKO 在一个所有人都被同化的扑克牌体制下选择反抗。
>
> 注意这只是一句话的自然语言输入，没有专业术语，没有技术参数。

**画面**：AI 回复出现，梳理了故事结构、主题定位和风格方向。

> AI 导演立刻提炼出了核心叙事：**觉醒 × 同化**，并确认了赛博电影的整体基调。
> 用户可以继续细化，也可以直接进入下一步。

---

## `01:02` 第二轮对话 · 定配乐

**画面**：用户输入：
> *"配乐用暗调 Synth-Cinematic，92 BPM，低频 bass 驱动，带失真女声人声"*

> 第二轮，我们锁定音乐风格。BPM、音色、人声处理方式，一句话说清楚。

**画面**：AI 回复确认音乐方案，列出 Suno v4 的具体生成参数。

> AI 导演确认使用 **Suno v4** 模型，自动将口语描述转化为精确的音乐生成 Prompt，
> 并与视频时长精确对齐——258 秒，每帧都有对应的音乐节拍。

---

## `01:12` 第三轮对话 · 定镜头

**画面**：用户输入：
> *"镜头：特写景深 + 跟踪运动镜头，色调冷蓝暗金高对比，电影级 LUT"*

> 第三轮，用户提出了电影级的镜头语言要求。
> 不需要知道这些技术参数的专业含义，AI 会帮你翻译成每个分镜的摄像机指令。

**画面**：AI 回复列出 7 个分镜的摄像机方案和模型分配。

> 你可以看到 AI 导演为每个场景独立选择了最合适的生成模型：
> - 极近景 + 景深 → **Veo 3.1**
> - 监控鱼眼视角 → **Grok Video**
> - 爆炸慢动作 → **Seedance 2.0**
>
> 这是 AIMV 区别于其他工具的关键：**不是单一模型，而是模型编排**。

---

## `01:22` 点击「生成 MV」

**画面**：用户点击「生成 MV」按钮，聊天区出现完整分镜方案。

> 现在点击生成。
> AI 导演基于三轮对话，自动输出了完整的创作方案：
> **2 个角色档案 + 7 个分镜场景 + 1 套音乐方案**，全部结构化，随时可以微调。

---

## `01:28` 展示分镜方案

**画面**：缓慢滚动查看 7 个场景卡片，每张卡片包含场景名称、时间轴、镜头描述和模型推荐。

> 我们来看分镜方案的细节。
>
> 每个场景都有：
> - 精确的**时间轴**（起止秒数）
> - 详细的**视觉描述**和**摄像机指令**
> - 对应的**AI 模型推荐**
> - 角色一致性锚点——保证 EIKO 在所有场景中外观一致
>
> 这份方案可以直接用于专业的分镜脚本，也可以导出给真人导演参考。

---

## `01:47` 生成进度面板

**画面**：右侧弹出 ProgressPanel，依次出现：
- 🎵 音乐生成中 → ✅ 音乐完成
- 🎬 片段 1 生成中（凝视）→ 出现缩略图 ✅
- 🎬 片段 2（流水线）→ ✅
- 🎬 片段 3（炸弹）→ ✅
- ... 共 7 个片段逐一完成
- 🔀 合成中 → ✅ 完成

> 生成正式开始。
>
> 可以看到进度面板实时更新每一步状态：
> **音乐先行生成**，之后 7 个视频片段并行调度，每完成一个就实时显示缩略图。
>
> 每个片段背后，平台自动完成了：图像参考生成、视频生成、质量评分、片段拼接——
> 这是一条完整的 AI 制片流水线，用户只需要等待。
>
> *(约 1 分钟后)*
>
> 所有片段生成完成，最后进入合成阶段，将音频与视频精确对齐。

---

## `03:01` 「你的 MV 已完成！」

**画面**：Studio 切换到完成态，"你的 MV 已完成！"横幅出现，视频播放器加载完毕。

> 完成了。
>
> 从一句话描述，到完整的 MV，总共经历了三轮对话和不到两分钟的生成等待。
> 这部 MV 叫做**《觉醒协议》**，是 EIKO 的故事。

---

## `03:05` 播放成片（约 1 分钟）

**画面**：视频从头播放，分三段展示：开头、中段高潮、逃离结尾。

### 第一段 `03:05`：开场凝视

> 看这个开场镜头——EIKO 缓缓睁开双眼，极浅景深，瞳孔里映出冷光。
> 这是 Veo 3.1 生成的，完全来自 AI。

### 第二段 `03:25`：爆炸崩塌

> 中段：炸弹倒计时，办公室崩塌，扑克牌如雪花四散。
> 这个慢动作爆炸场景使用的是 Seedance 2.0，专门针对动态场景优化。
> 注意背景音乐——这是 Suno v4 根据我们第二轮对话生成的，BPM、低音、人声全部对上了。

### 第三段 `03:45`：逃离虚空

> 结尾：EIKO 在深红虚空中，周围漂浮被撕碎的扑克牌残片——
> 这是对这个世界的终极告别，也是整个三幕叙事的高潮。

---

## `04:05` Canvas 节点图

**画面**：切换到 Canvas 页面，展示 VueFlow 节点图：音乐节点、角色节点、7 个片段节点，点击每个节点显示详情面板。

> 最后展示 AIMV 的另一个核心功能：**Canvas 可视化编辑器**。
>
> 每一部 MV 在生成完成后都会自动构建这张节点图：
> - **音乐节点**：对应 Suno 生成的音轨，可以替换或重新生成
> - **角色节点**：EIKO 的外观定义，修改后所有相关场景同步更新
> - **片段节点**：每个分镜的生成结果，点开可以看到原始 Prompt、质量分、缩略图预览
>
> 这不是一个只能"生成然后等结果"的黑盒工具——
> **你可以在节点图里修改任何一个分镜，只重新生成那一片段，而不是整部片子重来。**

**画面**：逐一点击 7 个片段节点，每次显示该节点的视频缩略图和详情。

> 这是"凝视"节点——EIKO 特写，Veo 3.1 生成。
> 这是"炸弹"节点——监控视角，Grok Video 生成。
> 这是"标志"节点——片尾 EIKO 徽标，极简定格。
>
> 每一帧素材的来源、模型、参数都完整可溯。

**画面**：缩放拉远，显示完整节点图全貌。

> 这是一部完整 MV 的"分子结构"。每个节点可以单独替换，可以分享给协作者，也可以导出为专业制片文档。

---

## `04:50` 结尾（视频结束后说）

> 总结一下今天的演示：
>
> - **输入**：3 句自然语言，30 秒
> - **规划**：CrewAI 多 Agent 自动完成分镜、角色、音乐方案
> - **生成**：多模型并行调度，Veo / Seedance / Suno 按场景最优分配
> - **输出**：一部完整的专业级 MV，全程约 5 分钟
>
> AIMV 解决的不是"AI 能不能做视频"的问题——这个问题已经解决了。
> **我们解决的是：怎么让一个没有任何制作经验的人，用 AI 做出有结构、有叙事、有情感的 MV。**
>
> 谢谢大家。

---

## 附：常见追问参考

| 问题 | 参考答案 |
|------|----------|
| 生成时间多久？ | Demo 中约 90 秒（Mock 加速），实际模型调用约 3–8 分钟（取决于片段数量和模型队列） |
| 费用怎么算？ | 按生成 Token 和视频秒数计费，7 个片段约消耗 $2–5 的 API 成本 |
| 可以上传自己的音乐吗？ | 支持。上传后 AI 会分析 BPM、节拍点，自动对齐每个分镜的时间轴 |
| 角色一致性怎么保证？ | 通过 Character Bank 中的 LoRA ID 锁定外观，每个视频 Prompt 都注入角色参考 |
| 能导出哪些格式？ | MP4 成片、SRT 字幕、分镜 PDF、节点图 JSON（可导入其他工具继续编辑） |
| 支持哪些模型？ | 目前接入：Veo 3.1 / 3.0 / 2.0、Seedance 2.0、Grok Video 1、Suno v4、Imagen 3、Gemini Flash |
