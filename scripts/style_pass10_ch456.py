# -*- coding: utf-8 -*-
"""pass10 续：改写第4-6章正文（46段）。跳过代码块/ASCII图。逐字保留数字/API/字段/引用。"""
from docx import Document
PATH = "毕业论文_新.docx"

R = [
("首先核算经济成本",
 "先算经济成本。开发阶段就是个人毕设，靠开源软件栈加上闭源 API 的免费额度，核心功能跑起来几乎不花钱。真要投生产，开销大致三块。"
 "一块是 LLM 推理：以 GPT-4o 算，跑完一次完整规划链路（Screenwriter、Director、Music Producer，再加几次 Verifier）大约 8000 input 加 4000 output token，"
 "按现在的 API 报价，单次约 0.06 美元。一块是图像、视频、音乐生成：一条 MV 一般 4–6 个分镜，全走闭源 API 总价约 0.8–1.5 美元，自托管开源模型的话单次电费不过 0.05–0.10 元。"
 "还有一块是基础设施——服务器、对象存储、网络带宽，按月固定支出。三块合起来估，闭源模式下生产环境一条 MV 约 6–10 元人民币，开源模式约 1–2 元，远低于传统 MV 动辄数千上万的单条成本。"
 "推理这块靠的是实习期间在共绩算力部署的千卡 RTX 4090 集群，日后投生产，通过对接闭源 API 加 GPU 集群就能弹性控成本。"),

("技术可行性方面，系统所用的 Vue 3",
 "技术上，系统用的 Vue 3、FastAPI、Celery、LangChain、PostgreSQL、Redis、MinIO、FFmpeg 都经过工业界大规模检验，单个组件可靠性不用担心。"
 "真正的风险有两处：一是各家模型 API 稳定性参差不齐，得靠 ModelRouter 容灾切换来缓；二是视频生成时延不好预测，得靠 Celery 异步管线配 WebSocket 进度推送来掩盖。"
 "这两类风险本设计都有招。AI 模型层走统一的适配器接口接入，新增一个模型只要实现 generate(prompt, init_image, duration) 和 extract_last_frame(video_url) 两个方法，从适配到上线一般 1–2 个工作日就能完成。"),

("组织层面同样易于落地",
 "组织层面也好落地。Web 工作台沿用了 Notion、Figma、Runway 这些主流 SaaS 的交互范式，上手门槛不高；目标用户——独立音乐人、短视频创作者、广告创意从业者——本来就熟 Web 工作台，不用额外培训。"
 "借 Docker Compose 一键部署，就算没运维背景的开发者，本地 30 分钟也能把整套服务搭起来，私有化部署难度不大。"),

("再说法律上的可行性",
 "法律上也站得住。本系统遵守《中华人民共和国著作权法》《计算机软件保护条例》《生成式人工智能服务管理暂行办法》[20] 以及《互联网信息服务深度合成管理规定》，对应的合规要点见表 4.1。"),

("Wan2.2 14B 遵循 Apache 2.0 协议",
 "Wan2.2 14B 用的是 Apache 2.0 协议，ACEStep 1.5 用 MIT 协议，闭源 API 则通过官方付费授权来用。"),

("在用户画像与需求映射上",
 "用户画像和需求映射上，系统主要面向三类目标用户，他们各自的痛点和对应的功能模块见表 4.2。"),

("这三类用户的共同需求可归纳为四条",
 "这三类用户的共同需求，说穿了就四条：门槛要低，不用会专业剪辑或设计；出片要快，几十分钟内得拿到成片；质量要可控，不能动不动冒出“AI 味十足”的崩坏镜头；"
 "分发要覆盖多平台，一次生成、多版本输出。系统所有核心设计都围着这四条转。"),

("图 4.1 把功能分成了创作、生成、合成、分发四个层次",
 "图 4.1 把功能分成创作、生成、合成、分发四层。清单不短：用户登录与注册（含访客模式）、项目管理（CRUD 和级联清理）、对话式分镜规划（SSE 流式、跟 AI 导演交互）、"
 "音频上传与音乐分析、单模态生成（图像、视频、音乐各自单独触发）、端到端 Pipeline、A/B 模型对比、Verifier 自动复检、"
 "多平台导出（抖音 9:16 / B 站 16:9 / YouTube HQ / 小红书 3:4 / Instagram Reels）、公开画廊与点赞，再加上 WebSocket 实时进度推送。"),

("非功能需求亦需说明",
 "非功能需求也得说。安全这块，靠登录限速、文件大小校验、Pydantic 校验和 BOLA 防护几道关卡守着；扩展性上，所有外部模型都走统一的适配器接口接入；"
 "可用性上，凡耗时任务一律异步执行；可移植性上，Docker Compose 一键就能启动。性能则得如实讲：本系统目前只做了开发期和小规模并发测试，还没上工业级压测，"
 "所以对高并发延迟不作承诺，7.4 节会给出单次端到端生成的时延分解当性能依据。"),

("本章从经济、技术、组织、法律这四个维度",
 "本章从经济、技术、组织、法律四个维度论证了系统的可行性，又从用户、功能、非功能三个层面给出了需求规格，为后面的设计提供依据。"),

("首先说明设计目标。用户应能在 5 分钟内",
 "先说设计目标。用户应该能在 5 分钟内完成首次注册、跑通一次 MV 生成；给一句创意就能拿到一支含 4–6 个分镜的成片；能上传自有音频替换 AI 生成的音乐；"
 "支持多个模型做 A/B 对比；支持五大主流平台导出；靠 Docker Compose 一键启动；公开画廊则允许访客浏览。"),

("再说明总体架构",
 "再说总体架构。系统工程上是“前端 + API + 异步 Worker + 模型适配器 + 数据/存储层”五层，各层职责见表 5.1。"),

("由表 5.1 可见",
 "如表 5.1，系统通过适配器层把方法层和具体模型解耦——任何模型的替换或新增都不影响业务层代码。"),

("整套系统在功能上分为四大模块",
 "系统在功能上分四大模块：用户与项目管理、创作与生成、合成与导出、画廊与运营。这四个模块在数据流上按“用户与项目 → 创作与生成 → 合成与导出 → 画廊”的先后依赖走。"),

("整条路径由 5 个用户可见的状态串联",
 "整条路径由 5 个用户可见的状态串起来，每个状态对应后端的一类 API，整个流程通过 WebSocket 实时回传进度。"),

("再说明项目状态机的定义",
 "再说项目状态机。项目的生命周期由后端的 Project.status 字段表示，状态转移见表 5.2。"),

("每个状态对应特定的前端 UI 与可执行操作",
 "每个状态对应特定的前端 UI 和可执行操作：draft 阶段只显示对话框和“生成分镜”按钮；planning 阶段拿骨架屏加进度文字让用户耐心等；"
 "planned 阶段允许预览分镜、调时长、改提示词；generating 阶段展示每镜的实时缩略图和进度条；done 阶段进后期编辑器和导出界面；failed 阶段则显示失败原因和重试按钮。"
 "状态机由后端单点维护，前端只按 WebSocket 推送被动更新，这样就避开了前后端各维护一套状态机闹不一致的风险。完整的状态转移见图 5.1。"),

("结合图 5.1，正向链路",
 "对照图 5.1，正向链路（蓝色）覆盖项目的标准生命周期，所有异常路径（红色）都收敛到 failed 状态；用户通过重试链路（橙色虚线）就能退回失败前的上一个状态接着干。"
 "这种“显式失败、还能恢复”的设计，避开了“项目假死”或“不知卡哪了”的体验断层。"),

("再说明异常的回退路径",
 "再说异常的回退路径。失败处理守着“局部失败、不阻塞全局”的原则。单个 Agent 失败，就把该 Agent 节点标记失败、重试整条规划链路；"
 "单个分镜的图像或视频生成失败，要么由 Verifier 触发反思分支，要么最终拿占位帧补上，整条 Pipeline 照常往前；合成阶段失败（比如 FFmpeg 异常），"
 "就把已生成的素材都保住，让用户在后期编辑器里手动重新合成。这些失败全记到 Task.error_log 字段，方便事后排查。"),

("再说明并发控制",
 "再说并发控制。同一个项目在 generating 状态下不许再触发新的 Pipeline——后端会在状态机层直接拒掉重复请求、返回 409 Conflict；"
 "同一个用户在不同项目之间可以并发触发 Pipeline，但全局 Celery worker 池按 FIFO 调度，保证不会因为单个用户大量并发就把集群占满。"
 "WebSocket 连接以 (user_id, project_id) 为索引，同一项目最多保持 1 个活跃连接，再连就关掉旧的，免得进度推送出现“幽灵广播”。"),

("从用户角色与用例来看，系统包含登录用户与访客",
 "从用户角色和用例看，系统有登录用户和访客两类角色，关系见表 5.3。"),

("为何选用 PostgreSQL",
 "数据库为什么选 PostgreSQL？因为它原生支持的 JSONB 适合存 plan dict 这类半结构化内容，asyncpg 异步驱动又跟 FastAPI/SQLAlchemy 的异步天然合拍，而且 ACID 事务和扩展性都比同类产品强。"),

("系统主要包含 5 个核心实体",
 "系统主要有 5 个核心实体：User、Project、Task、Media、GalleryLike。它们之间的关系见图 5.2。"),

("由图 5.2 可见，所有业务数据均以 User 为根",
 "如图 5.2，所有业务数据都以 User 为根，沿“User → Project → Task / Media → GalleryLike”分两层级联；任何二级实体的查询都得先过 user_id 校验。"
 "这正好是 4.2 节 BOLA 防护在数据库层面的天然支撑。用文字表述的关系简图如下："),

("关键的设计在于：所有业务实体的查询，都以 user_id 为起点",
 "关键设计在于：所有业务实体的查询都从 user_id 起头，一级一级往下延伸，每一层都强制校验所属用户，由此撑起 4.2 节的 BOLA 防护。"),

("User 表存用户名、bcrypt 密码哈希",
 "User 表存用户名、bcrypt 密码哈希、邮箱和角色；Task 表存 stage（image/video/music/compose/export）、segment_id、celery_task_id、status、score、retry_count；"
 "Media 表存 type、MinIO URL，还有 meta（duration / resolution / bpm 等 JSONB）；GalleryLike 表则存 (user_id, project_id) 的点赞关系。"
 "表结构和索引是靠 Alembic 三次迁移定下来的：001 建 users / projects，002 建 tasks / media / gallery_likes，003 在 Media.project_id 上加一个 B-tree 索引。"),

("再说明 JSONB 字段的策略",
 "再说 JSONB 字段的策略。plan 和 meta 只在写入和整体读取时被访问，基本不参与 WHERE 筛选，所以不用额外建索引；日后真要“按 storyboard 分镜数量筛选”，再加一个 GIN 索引就行。"),

("本章给出了系统的设计目标、五层总体架构",
 "本章给出了系统的设计目标、五层总体架构、四大功能模块、业务流程、用户角色用例和数据库设计，每个设计点都附了一句“为什么这么设计”。"),

("首先说明设计缘由。用户认证采用基于 JWT",
 "先说设计缘由。用户认证走基于 JWT 的无状态机制，后端任何一个节点都能独立验证身份；项目模块的核心则在“完整的生命周期管理”，里头包括级联删除时对 MinIO 对象的清理。"),

("实现要点如下。注册时以 Pydantic 校验用户名",
 "实现要点这么几条。注册时拿 Pydantic 校验用户名（2–50 字符）和密码（不少于 6 字符），密码经 bcrypt 哈希后存；登录时签发 JWT，"
 "再借 Redis 的 INCR 和 EXPIRE 做到每个 IP 每分钟限 10 次；删项目时，先把该项目下所有 media 记录批量列出来，调 MinIO 客户端的 remove_objects 删对象，最后才删数据库记录，免得 MinIO 里留下孤立文件。"),

("由图 6.1 可见，登录与注册采用左侧文字介绍",
 "如图 6.1，登录和注册是左侧文字介绍、右侧表单的双栏布局，输入项只有用户名和密码两项，正好贴合 AIMV Studio “快速进入”的产品定位。"),

("由图 6.2 可见，项目卡片在缩略图",
 "如图 6.2，项目卡片在缩略图、标题、状态徽章、更新时间这四类信息之间排了视觉权重，方便用户一眼认出项目状态。"),

("首先说明设计缘由。创作工作台是用户接触",
 "先说设计缘由。创作工作台是用户接触多智能体规划链路的主入口；它的关键设计是把复杂的 Agent 协作藏在 SSE 流式对话后面，让用户感觉是“正在跟一位 AI 导演聊天”，而不是“正在调一个生成 API”。"),

("工作台包含左侧聊天面板、中部预览区",
 "工作台分左侧聊天面板、中部预览区、右侧时间线与道具面板，运行效果见图 6.3。"),

("由图 6.3 可见，工作台左侧的聊天区",
 "如图 6.3，工作台左边的聊天区和右边的分镜时间线构成一个 split-view：左边管“语义层规划”（跟 Agent 对话），右边管“结构层规划”（确认分镜的时长和提示词），把“构思”和“查看”分两边放，方便快速迭代。"),

("工作台通过两条 API 与后端通信",
 "工作台通过两条 API 跟后端通信：POST /projects/{id}/chat（SSE 流式）供用户跟 AI 导演对话；POST /projects/{id}/pipeline/start 用来触发完整的 Pipeline。"
 "用户一点“生成分镜”，规划服务就串行跑 Screenwriter → Director → Music Producer 三个 Agent，最后把三者的输出合成一个 plan dict 写回数据库。"),

("首先说明设计缘由。Pipeline 的工程难点",
 "先说设计缘由。Pipeline 的工程难点不在单步生成，而在“多步异步任务的依赖编排和失败恢复”。本文拿 Celery chord 原语让“图像并发和音乐并发”在同一时间窗里跑完，再靠 chord 的 callback 自动触发“视频帧链路串行”。"),

("run_video_chain 会逐一遍历每个分镜",
 "run_video_chain 会一个个遍历每个分镜，调 ShotRouter 选视频模型，把上一镜头的末帧当下一镜头的 init_image："),

("再说明 Celery chord 的依赖图设计",
 "再说 Celery chord 的依赖图。本系统的任务拓扑可以抽象成三层依赖：最底层是“图像生成和音乐生成”的并发组（group）；往上一层是要等第一层全完成的“视频帧链路串行”（也就是 chord 的 body）；"
 "最上层则是要等视频链跑完的“合成与字幕烧录”。完整依赖见图 6.4。"),

("由图 6.4 可见，并行任务（图像生成与音乐生成）",
 "如图 6.4，并行任务（图像生成和音乐生成）经 Chord 的 fan-in 同步点，全部完成后再触发 4 个串行的视频任务，最后进合成节点。Chord 是这套拓扑的关键原语："
 "它的特点是“group 没全完成就不触发 body”；要是改用 apply_async 加轮询去手动实现同样效果，还得额外维护任务状态表和回调注册表，代码会复杂不少。"
 "而 chord 内部只靠一个 Redis 计数器就实现了 fan-in 同步，业务层根本不用插手。"),

("再说明任务投递的可靠性",
 "再说任务投递的可靠性。Celery 默认是 at-most-once 语义（任务可能丢），本系统改成 at-least-once。一来所有任务函数都做成幂等的"
 "——拿 task_id = f\"{project_id}:{stage}:{segment_id}\" 当业务幂等键，重复执行同一个 task_id 时先查数据库里有没有结果，有就直接返回、不再重新生成。"
 "二来 Celery worker 配 acks_late=True，消息要等任务执行完才 ack，免得 worker 崩了任务跟着丢。"
 "三来开了 RabbitMQ 的死信队列（DLQ）：任务连续失败 3 次就转进 DLQ 交人工排查，免得无限重试占着 worker。"),

("再说明进度推送的链路",
 "再说进度推送的链路。信号从 Celery worker 到前端浏览器要走三跳。Worker 在每个关键节点（任务开始、完成、失败回退）把消息发到 Redis Pub/Sub 频道 progress:{project_id}；"
 "API 层给每个项目维护一个 Redis 订阅协程，把消息转给对应的 WebSocket 连接；前端再通过 Pinia store 收事件、刷 UI。整条链路的延迟一般 100–200 ms，比起分镜级 30–90 s 的耗时几乎可以忽略。"
 "至于这里为什么用 Redis Pub/Sub 而不是更复杂的 NATS / Kafka，是因为本系统的进度消息属于“瞬时事件”，不用持久化和回放，Pub/Sub 的 fire-and-forget 模型正好合适。"),

("长任务的取消与断点续传亦经专门处理",
 "长任务的取消和断点续传也专门处理了。用户点“取消生成”，前端先调 POST /pipeline/{project_id}/cancel，后端再通过 Celery 的 revoke 机制把任务 ID 加进 revoke 列表；"
 "已经在执行的任务会在下一个检查点（也就是每生成一镜之后）查一下 revoke 标志位、主动退出。已生成的镜头还留在数据库里，用户重启 Pipeline 时凭幂等键复用，相当于天然的断点续传。"),

("整条 Pipeline 通过 Redis Pub/Sub 加 WebSocket 实时回传进度",
 "整条 Pipeline 通过 Redis Pub/Sub 加 WebSocket 实时回传进度。Pipeline 跑完，系统在后期编辑器里给出 A/B 对比和导出界面（见图 6.5）；"
 "图 6.6 则展示了系统针对一段“未来都市霓虹少女”创意生成的若干连续分镜帧。"),

("由图 6.5 可见，后期编辑器将",
 "如图 6.5，后期编辑器把“对比择优”和“多平台导出”放在同一个视图里，方便用户确认最佳版本后立刻一键分发。"),

("由图 6.6 可见，主角的发型",
 "如图 6.6，主角的发型、服饰、风格在若干连续分镜里都保持得高度一致，印证了 3.5 节末帧链路加角色库锚点的实际效果；不过远景切近景时仍有轻微的面部漂移，"
 "跟误差累积分析的预期吻合，这点留到 8.2 节当局限性讨论。为了更直观地展示跨镜头一致性，图 6.7 到图 6.10 进一步给出“国风古典”风格下系统生成的 4 个连续分镜帧"
 "——同一主角（淡粉汉服、金色腰带、长黑发）依次出现在云海远景、桃花中景、含泪近景、夕阳石桥四个场景里。"),
]


def set_text(p, text):
    if p.runs:
        p.runs[0].text = text
        for r in p.runs[1:]:
            r.text = ""
    else:
        p.add_run(text)


d = Document(PATH)
done = set()
for p in d.paragraphs:
    t = p.text
    for i, (snip, new) in enumerate(R):
        if i in done:
            continue
        if snip in t:
            set_text(p, new)
            done.add(i)
            break

missing = [(i, R[i][0][:22]) for i in range(len(R)) if i not in done]
if missing:
    print("[WARN] NOT matched:", missing)
else:
    d.save(PATH)
    print("saved", PATH, "| 共替换", len(done), "段")
