# -*- coding: utf-8 -*-
"""pass10 续：批量改写 1.4 / 1.5 / 第2章全部正文段。
按唯一子串定位整段替换，保留首run格式。引用/型号/数字/章节图表引用全部保留。"""
from docx import Document
PATH = "毕业论文_新.docx"

# (定位子串, 新文本)
R = [
("笔者的主要工作可以归结为以下三项创新",
 "前面 1.1 节列的三个痛点，正好对应笔者做的三项主要工作，下面逐一说。"),

("创新点 1 是一套四角色 Agent",
 "创新点 1 是一套四角色 Agent 串行规划、辅以 Verifier 复检的多智能体协作范式。把传统 MV 制作里“剧本—视觉—音乐—质检”这四件事，"
 "分别交给 Screenwriter、Director、Music Producer、Verifier 四个 LLM 智能体，用 LangGraph 串起来依次跑。它和“一个 Agent 包打天下”的写法不是一回事，"
 "差别主要落在三处：职责怎么对应、上下文怎么传、出错了怎么退回重来。消融实验也印证了这一点——角色一致性和提示词契合度两项，多角色方案都更好。"),

("创新点 2 是一套基于 MFCC",
 "创新点 2 是一套基于 MFCC + chroma 自相似矩阵聚类的音乐结构对齐方法。做法大致是这样：先在帧级抽 13 维 MFCC 和 12 维 chroma，拼成 25 维特征，"
 "用余弦相似度搭出自相似矩阵，再用层次聚类把段落边界找出来；然后按 RMS 能量给每段贴上 intro / verse / chorus / bridge / outro 的标签，"
 "当作硬约束塞进 Screenwriter 的任务描述里。效果挺明显——消融实验里节拍对齐指标 Beat-F1 从 0.41 涨到了 0.78。"),

("创新点 3 是将末帧链路",
 "创新点 3 把末帧链路和双轨路由凑到一起，用来保多镜头的视觉一致性。最关键的动作其实就一个：把上一镜头的末帧，直接拿去当下一镜头视频生成的初始参考帧；"
 "这样从第一帧起，相邻镜头就有了像素级的承接，一环扣一环。配合它，本文还做了个 ShotRouter，把演唱镜头和叙事镜头各自分到擅长的模型上去，免得一个模型两头都不讨好。"
 "用户研究里，这套机制把“角色一致性”这项的 MOS 抬高了 0.5 分。"),

("对应的方法章节、对应的实验验证之间的闭环关系",
 "这三项创新点跟 1.1 节的三个痛点、各自对应的方法章节和实验验证之间是怎么扣起来的，列在表 1.2 里。"),

("支撑起论文的严谨性",
 "表 1.2 里看得很清楚，三项创新和背景里那三个痛点是一一对应的：每一项都在第 3 章讲了方法、又在第 7 章的消融里单独拿到了指标验证。"
 "撑起这篇论文严谨性的，正是这条“痛点—方法—验证”的闭环。"),

("全文结构安排如下",
 "全文这样安排。第一章先把研究背景、国内外现状、开发目标、创新点和论文结构交代清楚。第二章讲开发环境、相关理论和工程技术。"
 "第三章是方法的重头戏，核心在 3.4 节“多智能体规划链路”——从形式化建模、四个角色为什么这么拆，讲到 LangGraph 怎么编排、上下文怎么传，"
 "再跟单 Agent 范式比一比，一层层往下走。往后，第四章是可行性和需求分析，第五章给系统总体设计，第六章落到各模块怎么实现，"
 "第七章包含功能测试、定量评估、消融、基线对比、用户研究和时延分解；第八章收尾，做工作总结、局限性分析和未来展望。"),

("前者是方法赖以成立的根基理论",
 "本章先说开发环境，再分“理论”和“工程”两块讲：前者是方法立得住的根基，后者是系统落得了地的手段。"),

("从用户终端一直到底层数据，共分为五层",
 "系统的开发栈从用户终端一直铺到底层数据，一共五层，整体关系见图 2.1。"),

("这五层之间的解耦依赖于层间标准化的接口",
 "五层之间能解耦，靠的是层与层之间那套标准化接口：前端只跟 API 层的 RESTful 打交道，API 层只认异步层的消息队列约定，"
 "异步层往下只面对 Agent 层的 Adapter 接口，AI 模型层再往下只碰数据层的对象存储和 ORM 模型。这样一来，想换掉其中任意一层"
 "——比如把 Vue 换成 React、把 PostgreSQL 换成 MySQL——都不会牵连到别的层。系统的核心开发环境见表 2.1。"),

("整套服务通过 Docker Compose 一条命令即可启动",
 "整套服务用 Docker Compose 一条命令就能拉起来。模型推理这块，靠的是作者实习期间在共绩算力部署的那套千卡 RTX 4090 集群，"
 "每张卡 24 GB 显存——单卡就扛得住 Wan2.2 14B 和 ACEStep 1.5 推理时的显存峰值，整个集群的规模也够好几个用户同时跑生成。"),

("在零样本与少样本条件下兼具自然语言的理解",
 "大语言模型（LLM）说到底，是以 Transformer [1] 为底座、在万亿级 token 语料上练出来的一类深度学习模型，"
 "零样本和少样本下都能理解、生成和推理自然语言 [2]，GPT-4o、Claude、Gemini、通义 Qwen 都属于这一类。"
 "到了多智能体场景，LLM 的身份变了：它不再是回答一次问题的机器，而是一个被赋予了角色和目标的通用决策核。"
 "每个 Agent，其实就是把 LLM 做了一次“角色化包装”。"),

("指的是让多个角色不同、能力各异的智能体协同",
 "多智能体协作（Multi-Agent Collaboration），讲的是让一群角色不同、本事各异的智能体一起把一件复杂任务做完 [16, 19]。"
 "这里有个绕不开的现象：任务一旦掺进多种异质决策，单个智能体很容易在长上下文里“角色漂移”——输出慢慢跑偏，离它本该守的职责越来越远，"
 "Hong 等人 [13] 已经拿实验证过。多智能体的解法，是给每个智能体划好独立的职责边界和输出约束，从源头上不让职责混起来。"
 "Du 等人 [15] 还更进一步：让几个独立智能体各自推理、再互相审，单个智能体那种事实性错误能压下去不少。"),

("多智能体的协作大致可归为四种组织方式",
 "把近几年的研究 [12, 15, 17] 放一起看，多智能体的组织方式大致有四种。最朴素的是串行——前一个的结果原样递给下一个，一棒接一棒，"
 "适合有先后依赖的任务。并行正相反，几个智能体各答各的，再投票或集成挑出最好的。分层则像个调度器，由一个总智能体动态地把活儿派给手下。"
 "剩下反思，是同一个智能体反复看自己的输出、不停改，直到达标。本系统规划阶段走串行，因为剧本、视觉、音乐这三类决策本就得按顺序来，"
 "后面的智能体得先读到前面的结果才动得了；到了质量复检阶段则换成反思，让质检智能体对生成内容反复打分，不达标就重试。"
 "落到工程上，本文用的是 LangGraph——它是 LangChain 生态里的多智能体编排框架，用状态图来组织智能体的执行次序，扩展和维护都比较省心。"),

("到了视频生成，就在图像扩散的基础上",
 "扩散模型 [3, 4] 是把图像生成当成一个“一步步去噪”的反向过程来做。轮到视频生成，就在图像扩散之上再加一个时间维度，"
 "常见的是时空 Transformer，Sora 的 DiT 架构 [22] 就属于这类。本系统接进来的 Veo 3.1、Seedance 2.0、Wan2.2 14B、CogVideoX、Grok Video，"
 "底子都是扩散类架构，差别主要在条件控制和首尾帧引导能力上。"),

("指从音频信号中自动提取节拍、段落、人声、歌词",
 "音乐信息检索（Music Information Retrieval，MIR），是从音频信号里自动抠出节拍、段落、人声、歌词这些结构化信息，而这些在 MV 制作里一个都不能少。"
 "打个比方：系统要是认不出副歌从第 48 秒起，就没法把高潮画面准准切到那个点；要是转不出歌词，字幕也对不上口型。"
 "所以本文拉了三个比较成熟的开源工具搭着用——librosa [9] 负责抽节拍时间序列和全曲速度（BPM），htdemucs [7] 把人声和伴奏分开、给后面各步一个干净的输入，"
 "faster-whisper [8] 则借 Whisper 模型把人声自动转写成歌词。在这基础上，本文又另搭了一套方法，靠音频特征的相似度去定段落边界，"
 "把歌切成主歌、副歌这些语义片段，好让分镜时长精确对齐（详见 3.3 节）。"),

("为避免本节沦为一份",
 "为了不把这一节写成“工具说明书”，下面只挑方法实现里真正要紧的工程技术来讲，而且每一项选型都附一句“为什么用它不用别的”。"),

("前端采用 Vue 3.4",
 "前端用 Vue 3.4（Composition API），搭上 Element Plus、Pinia、vue-i18n，做成一个中英双语的工作台。没选 React 而选 Vue 3，最看重的一点是："
 "在多智能体进度推送、A/B 对比、时间线编辑这类交互密集的场景里，Composition API 组织响应式状态比 React 的 hooks 直观得多。"
 "Element Plus 自带一整套中文化的企业级组件，对话气泡、表单校验、消息提示这些基础件不用自己写，省下不少时间。"
 "再加上 Vue 3 的单文件组件（SFC）把模板、脚本、样式收在一处，一个人维护全栈也好弄。状态管理用 Pinia 没用 Vuex 4，"
 "图的是它原生支持 TypeScript 类型推导、API 更贴 Composition API、又没有 mutation 那套概念，负担更小；构建工具用 Vite 5 没用 Webpack，"
 "则是因为它原生 ESM 的启动方式能把冷启动压到秒级。"),

("后端采用 FastAPI，并配合 SQLAlchemy",
 "后端用 FastAPI，配 SQLAlchemy 2.0 的异步会话和 asyncpg。比起 Django、Flask，FastAPI 的好处主要在几方面：它原生支持异步，配 asyncpg 驱动，"
 "扛得住 MinIO 上传、LLM 调用这类 I/O 阻塞型 API 的高并发；它基于 Pydantic 自动做参数校验、生成 OpenAPI 文档，样板代码能省下大概八成；"
 "它的依赖注入又让“鉴权 + 数据库会话 + 当前用户”这些高频复用的组件，用装饰器就能反复拿来用。SQLAlchemy 2.0 的 async session 写起来跟同步 ORM 差不多，"
 "底层却是全异步，省去了协程上下文切换那点心智负担。Pydantic v2 的校验性能比 v1 快了大约 5–50 倍，对本系统里那些嵌套挺深的 plan/storyboard JSON 校验，帮助尤其大。"),

("异步管线采用 Celery 5",
 "异步管线用 Celery 5 + RabbitMQ + Redis。作为任务调度框架，Celery 比较适合那种“步骤不多、但每步都耗时、依赖关系还复杂”的任务；"
 "跟 arq、dramatiq 这些基于 asyncio 协程的轻量方案比，它原生就有 chord（多任务 fan-in）、chain（任务串接）、group（并行集合）三种高阶组合原语，"
 "正好对上本系统“图像和音乐并行 → 视频串行 → 合成”的结构。消息中间件挑了 RabbitMQ 而不是 Redis Streams，几个考虑：死信队列和重试退避都是内置的，不用自己拼；"
 "Quorum Queue 对消息不丢的保证更好；管理面板看队列堆积、消费者吞吐也更清楚。Redis 则一身三用——既是 Celery 的 result backend，又是 Pub/Sub 消息总线，"
 "还兼着缓存和限速计数器，作为“通用辅助件”复用率最高。"),

("Agent 框架确定为 LangChain 0.3",
 "Agent 框架定的是 LangChain 0.3 加 LangGraph 0.2。在 AutoGen、CrewAI、Swarm、LangGraph 这几个主流框架里最后挑了 LangGraph，主要是几点考虑。"
 "它那套显式的状态图（State Graph）抽象，跟本系统“字段级访问控制”的需求特别合——每个节点对 State 的读写关系都能静态声明清楚。"
 "它还原生支持条件分支和反思循环，不用另写路由器节点，就能实现 Verifier 的反思跳转。再就是它跟 LangChain 生态打通得深，"
 "prompt 模板、output parser、callback handler 这一整套工具都能直接拿来用。还有一点，状态图原生支持序列化和断点续传，长任务万一中途挂了也好恢复。"
 "AutoGen 那种“对话式多 Agent”在自由协作的场景下确实更强，但 AIMV Studio 的协作高度规则化，状态图显然比对话历史更对路。"),

("数据库采用 PostgreSQL 16",
 "数据库用 PostgreSQL 16。它原生支持 JSONB，拿来存半结构化的 plan dict 正合适；跟 MongoDB 比，关键好处有两个：跨 plan、media、task 几张表一起更新时，"
 "ACID 事务少不了；GIN 索引也留了后手，以后真要按 storyboard 字段筛，也不用迁到单独的文档数据库去。对象存储则选了兼容 S3 协议的 MinIO。"
 "比起直接对接 AWS S3，它好在两点：一来能私有化部署，开发期和小规模生产都合适；二来既然兼容 S3 协议，日后想换 AWS S3、阿里云 OSS 或腾讯云 COS，"
 "业务代码一行不用动，换个 endpoint 就行。"),

("媒体处理全部交由 FFmpeg 6.1",
 "媒体处理全交给 FFmpeg 6.1：拼接、混音、loudnorm 响度标准化、字幕烧录、平台导出格式转换，这些视频音频活儿都归它。"
 "没用 MoviePy、OpenCV-VideoIO 这类更高层的封装，有几个原因。硬件加速上，FFmpeg 比纯 Python 库更能打（NVENC、QSV 都用得上），大批量视频合成的吞吐高出一截；"
 "它的滤镜图（filtergraph）语法表达力也强，不少复杂的多轨混音、字幕烧录、平台预设转换，一条命令就下来了；再加上生态够老，各种边角情况基本都能在 StackOverflow 上翻到现成解法。"
 "本系统把 FFmpeg 的调用包成了 Python 函数，对上层给一个统一接口。"),

("本章介绍了开发环境与相关理论",
 "本章过了一遍开发环境和相关理论，其中多智能体协作的理论和典型模式着墨最多；工程技术只作了概要，但每项选型都补了一句“为什么用它不用别的”，给下一章的方法设计打个底。"),
]


def set_text(p, text):
    if p.runs:
        p.runs[0].text = text
        for r in p.runs[1:]:
            r.text = ""
    else:
        p.add_run(text)


d = Document(PATH)
done = set()
for p in d.paragraphs:
    t = p.text
    for i, (snip, new) in enumerate(R):
        if i in done:
            continue
        if snip in t:
            set_text(p, new)
            done.add(i)
            print(f"[ok] #{i}: {snip[:20]}")
            break

missing = [R[i][0][:20] for i in range(len(R)) if i not in done]
if missing:
    print("[WARN] NOT matched:", missing)
else:
    d.save(PATH)
    print("saved", PATH, "| 共替换", len(done), "段")
