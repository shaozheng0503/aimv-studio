# -*- coding: utf-8 -*-
"""pass10 续：改写 3.5-3.7 正文（28段）。跳过伪代码P211与含公式P238。逐字保留参数/命令/引用。"""
from docx import Document
PATH = "毕业论文_新.docx"

R = [
("子问题 3 的核心矛盾在于每一次视频生成都是一个独立的扩散过程",
 "先说思路。子问题 3 的核心矛盾是：每一次视频生成都是一个独立的扩散过程，初始噪声各不一样，同一个角色就容易“前后不像”。"
 "本文搭了两层机制：末帧链路让相邻镜头共享像素级的视觉上下文；双轨路由让不同语境的镜头各用更合适的模型，免得单一模型在两类语境下都不讨好。"
 "两层各自独立又互相配合——末帧链路管“相邻镜头视觉接不接得上”，双轨路由管“每个镜头有没有选对模型”。"),

("末帧链路的核心规则可一言以蔽之",
 "末帧链路的核心规则一句话就能说清：第一个镜头用 Director 给的图像 prompt 生成一张静态关键帧当起点；自第二个镜头起，"
 "每个镜头都拿前一镜头的视频末帧当参考起始帧来驱动视频生成。这条规则在运行时由下面这段伪代码实现："),

("再说明末帧提取的工程细节",
 "再说末帧提取的工程细节。系统用 FFmpeg 抽视频最后一秒里编码最稳的那个关键帧，而不是严格意义上的“最后一帧”——因为真正的最后一帧在 H.264 编码下常是高熵的 B 帧或 P 帧，"
 "画质容易被运动模糊带坏。抽帧命令是 ffmpeg -sseof -1 -i input.mp4 -vsync 0 -frames:v 5 -q:v 2 frame_%03d.jpg，从里面挑文件最大、也就是细节最丰富的一帧当末帧。"
 "这点工程改良让末帧的有效信息密度提升了约 20%，下一镜头的角色还原度也跟着好起来。"),

("再说明漂移的累积与抑制策略",
 "再说漂移的累积和怎么压。就算有末帧链路，每个独立的扩散过程还是会带进一点点视觉漂移：相邻镜头之间的角色一致性还行，可一旦超过 8 个镜头，累积起来的漂移就接近肉眼能看出来的程度了。"
 "系统为此上了两道抑制。一道是每 4 镜回看一次首帧：在第 5、第 9、第 13 镜头的位置，把“首镜末帧 + 前镜末帧”以双参考的形式喂给视频生成模型"
 "（模型支持多参考输入就一并送，不支持就把两帧拼成左右双联画再送），硬把累积漂移拉回起点。另一道是漂移触发监控：每生成一个新镜头，就跟首镜末帧算一次 CLIP 图像嵌入的余弦距离，"
 "一旦超过 0.25 就立刻触发“回看首帧”分支，不必等到固定步长。这个监控让系统能照着内容实际的漂移速度自适应地调回看频率，而不是“每 4 镜”一刀切。"),

("ShotRouter 依据镜头标签、视觉风格、目标时长三个维度选取",
 "ShotRouter 按镜头标签、视觉风格、目标时长这三个维度来选视频生成模型。完整的决策矩阵见表 3.4。"),

("首选、次选、兜底的划分依据作者在 30 条测试集上",
 "首选、次选、兜底怎么分，依据的是作者在 30 条测试集上给各模型画的能力画像。Seedance 2.0 面部细节稳，但叙事运镜偏弱，所以当 sing 首选；"
 "Veo 3.1 的电影感运镜出色，可人物近景偶尔有伪影，放 story 首选；Grok Video 1.0 在风格化（漫画/复古）画风上有独到优势，写实场景却一般，归 stylized/anime 首选；"
 "Wan2.2 14B 是开源模型，综合表现稳但生成更慢，拿来当通用兜底。"),

("负责在模型不可用时进行容灾切换。它将故障分为五类",
 "ModelRouter 是架在 ShotRouter 之上的一个“上层调度器”，模型不可用时负责容灾切换。它把故障分成五类，每一类配一套不同的切换策略："),

("与此同时，ModelRouter 还维护了三种全局策略",
 "与此同时，ModelRouter 还备了三种全局策略，供用户在工作台手动切换。"),

("其一为开源优先",
 "一种是开源优先：所有镜头默认用 Wan2.2 14B，闭源 API 只在 Verifier 触发回退时才启用，适合预算有限、或者对数据出境有顾虑的用户。"),

("其二为闭源优先",
 "一种是闭源优先：按 ShotRouter 决策矩阵走，开源模型只作兜底，适合追求最高画质的用户。"),

("其三为自动模式",
 "还有一种是自动模式（默认）：完全照 ShotRouter 决策矩阵、再结合 ModelRouter 的容灾策略动态选型，不用用户操心。"),

("再说明动态参数调节",
 "再说动态参数调节。要是 Verifier 对同一个镜头连着两次给低分，ModelRouter 不光换模型，还会自动调生成参数：cfg_scale 在 7.5 / 9.0 / 11.0 三档之间逐级往上提（加强提示词约束），"
 "temperature 从 0.7 依次降到 0.4、再降到 0.2（提升输出稳定性），seed 则每次重试都随机换。这种多维度一起调的做法，让不换模型时单次重试的成功率从约 50% 提到了约 75%。"),

("两者的整体关系，见图 3.3",
 "ShotRouter 决策矩阵和 ModelRouter 容灾切换，两者的整体关系见图 3.3。"),

("系统对模型的选择采用上下两层结构",
 "图 3.3 里能看出，系统选模型是上下两层：上层 ShotRouter 按“镜头标签 × 视觉风格”做静态决策；下层 ModelRouter 则在运行时针对五类故障各上一套容灾策略；"
 "用户还能在工作台上显式切“开源优先 / 自动 / 闭源优先”三种全局策略，在成本和质量之间做明确取舍。"),

("因此本文借鉴 LLM-as-Judge",
 "先说思路。AI 生成模型本身稳定性就一般，所以本文借了 LLM-as-Judge [18] 和 Reflexion [17] 的路子，单设一个独立的 Verifier Agent 来做多维度评分。"
 "跟“让生成模型自己评”比，独立 Verifier 的关键价值在于打破生成端的自我合理化——生成模型对自己刚产出的东西天生有偏好，让它来评分，无异于让裁判给自家选手打分。"),

("Verifier 在三个维度上各给出一个 1–5 分的整数评分",
 "Verifier 在三个维度上各给一个 1–5 分的整数。每个维度“5 分”和“1 分”的标准都写死在 Verifier 的 system prompt 里，免得 LLM 自己揣摩分数边界、打得忽高忽低。"
 "视觉质量（visual）：5 分是毫无瑕疵、构图和光线到电影级；4 分是有轻微伪影但不影响观感；3 分是有可见伪影但还能用；2 分是伪影明显、得重做；1 分是画面崩坏（人体扭曲、面部融化、背景失真这类）。"
 "角色一致性（consistency）：5 分是主角外貌、服饰、风格跟 character_bank 完全一致；4 分是发型或服饰有一处细微偏差；3 分是还能认出是同一角色但多处细节漂移；2 分是名字对得上、视觉上却不太像；1 分是角色完全不一致甚至缺失。"
 "提示词契合度（adherence）：5 分是画面把 prompt 说的情节和情绪完整呈现；4 分是主要情节有、次要细节缺；3 分是情节大体对应但氛围或构图偏了；2 分是画面跟 prompt 只有微弱关联；1 分是画面跟 prompt 完全没关系。"),

("用以说明扣分原因，该 comment 随后会被重试分支",
 "Verifier 打分的同时，还会给一段简短的 comment（不到 50 字），说说为什么扣分；这段 comment 随后会被重试分支当作“失败原因摘要”反馈给 Director（见 3.4.4 节的反思分支）。"),

("阈值 3.0 并非随意设定",
 "阈值 3.0 不是随手定的：在 30 条测试集上看，三个维度都不低于 3 分时（最低综合分 0.4×3 + 0.4×3 + 0.2×3 = 3.0），用户研究的 MOS 普遍在 3.5 以上；"
 "而只要任一维度低于 3，MOS 就容易掉到 3.0 以下。这个阈值还能拦住“两高一低”的伪优质镜头——比如视觉满分、角色却跟主角完全不符的情况，因为角色一致性偏低，综合分照样可能够不着阈值。"),

("而是按失败次数逐档采取不同策略",
 "Verifier 触发的重试不是简单“原样重做”，而是按失败次数逐档换策略（详见 3.4.4 节反思分支的四档处理）。除了反思分支本身，Verifier 和 ShotRouter 之间还共享历史评分："),

("当某个模型在最近 5 次调用中出现 2 次以上低分时",
 "某个模型最近 5 次调用里出现 2 次以上低分，ShotRouter 就暂时把它降到次选位置（即“短期黑名单”），直到下一次冷启动；"),

("当某一类标签（如 sing）连续 3 次调用均回退到 fallback",
 "某一类标签（比如 sing）连续 3 次调用都回退到 fallback 模型，ShotRouter 就暂时把这类标签的首选模型改成 fallback（即“运行时学习”）；"),

("当用户上传的音轨风格（如复古迪斯科）首次出现时",
 "用户上传的音轨风格（比如复古迪斯科）头一回出现，Verifier 会进入一个“探索期”——把评分严格度适度放低（阈值临时调到 2.7），免得冷启动阶段重试太频繁。"),

("使 ShotRouter 的决策矩阵从纯静态规则升级为",
 "这种“拿评分历史驱动模型选择”的轻量反馈环，让 ShotRouter 的决策矩阵从纯静态规则，变成一个能在运行时自适应调整的弱在线学习系统——不用重新部署，用着用着就慢慢贴近当前用户的偏好。"),

("存在公认的 self-evaluation bias",
 "Verifier 自己也是个 LLM，有公认的 self-evaluation bias [18]——LLM 倾向于给自己或同族模型生成的内容打高分。本系统拿三层措施去缓解。一是 Verifier 选了 GPT-4o-mini，"
 "跟生成端（Director、Screenwriter 用 GPT-4o）不完全同族。二是 Verifier 的评分会用 CLIPScore 这类客观指标交叉校验，要是客观指标明显低于 Verifier 给的分，就触发“二次复检”"
 "——另起一个温度为 0 的 Verifier 实例对同一镜头独立重评一次，取两次的平均。三是把用户研究的 MOS 跟 Verifier 的评分做相关性分析，当外部校准（详见 7.4 节）。"
 "得承认，本文并没把自评偏置彻底消掉，这也是 8.2 节要谈的局限之一。"),

("Verifier 三维评分加四档重试的完整闭环，如图 3.4 所示",
 "Verifier 三维评分加四档重试的完整闭环，见图 3.4。"),

("Verifier 的反思机制将单次生成",
 "图 3.4 把 Verifier 的反思机制讲清楚了：它把单次生成“过 / 不过”的判断，扩成一条有递进策略的回退链路——从成本最低的“换种子加调参”，逐级升到“换模型”“重写 prompt”，"
 "再到极少触发的“占位帧填充”，每一档都有明确的工程含义，不是简单重试。这样整条 Pipeline 既能尽力去修低质量镜头，又不会在某个单点失败时无限挂起。"),

("3.5、3.6 节，则给出了帧链路和 Verifier 的实现细节",
 "本章是全文方法的核心。3.1 节给出了 MV 生成的形式化定义，把它拆成四个子问题；3.2 节给出端到端的流水线；3.3 到 3.6 节分别冲着这四个子问题给出具体方法，"
 "其中 3.4 节是重点，把多智能体规划链路从形式化建模一直讲到上下文传递机制；3.5、3.6 节则给出帧链路和 Verifier 的实现细节。这一章的方法，构成了全文最重要的研究贡献。"),
]


def set_text(p, text):
    if p.runs:
        p.runs[0].text = text
        for r in p.runs[1:]:
            r.text = ""
    else:
        p.add_run(text)


d = Document(PATH)
done = set()
for p in d.paragraphs:
    t = p.text
    for i, (snip, new) in enumerate(R):
        if i in done:
            continue
        if snip in t:
            set_text(p, new)
            done.add(i)
            break

missing = [(i, R[i][0][:24]) for i in range(len(R)) if i not in done]
if missing:
    print("[WARN] NOT matched:", missing)
else:
    d.save(PATH)
    print("saved", PATH, "| 共替换", len(done), "段")
