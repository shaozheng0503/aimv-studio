# -*- coding: utf-8 -*-
"""pass10 续：改写 3.1-3.3 正文（17段）。跳过含公式的 P131。保留全部参数/引用/图表引用。"""
from docx import Document
PATH = "毕业论文_新.docx"

R = [
("3.5 节论述模型路由与帧链路",
 "这一章是全文方法的核心。3.1 节先把 MV 生成这件事用形式化的方式定义清楚，3.2 节给出整体流水线，3.3 节专门讲音乐分析和结构对齐，"
 "3.4 节是重点、讲多智能体规划链路，3.5 节讲模型路由和帧链路，3.6 节落到验证与重试机制。"),

("本文将生成过程划分为五个阶段",
 "先说设计思路。顺着 3.1 节拆出来的那几个子问题，本文把生成过程切成五个阶段——前置音乐分析、Agent 规划、并行内容生成、视频帧链路、合成，"
 "而且特意让每个阶段只对应一个子问题。整条流水线见图 3.1。"),

("这条流水线遵循一个原则",
 "图 3.1 这条流水线守着一个原则：输入只给一次，中间该并行的并行、该串行的串行，跑完再过一道质量复检，最后多平台输出。有几处设计要单独点一下。"
 "图像和音乐生成放在并行阶段，由 Celery chord 触发，就是想把能并行的步骤尽量塞进同一个时间窗里；视频生成则只能串行，因为帧链路得等前一镜头的末帧；"
 "Verifier 评分插在每个生成步骤之后，免得不达标的镜头拖累后面的分镜。"),

("本节改用另一种叙述方式",
 "这一节换个讲法。拿一条具体创意当例子——“我要一个国风 MV，主角是古装少女，风格飘逸唯美”——顺着它把系统从输入到成片的整条链路走一遍，"
 "每个环节为什么这么取舍，边走边说。"),

("第一个接手的是剧本智能体",
 "用户把这句话敲进去后，第一个接手的是剧本智能体（Screenwriter）。它先理解、再扩写：从这短短一句里抠出风格（国风、古典）、"
 "主角外貌（青丝长发、汉服飘带）和情绪基调（思念、空灵），然后照着常见 MV 的叙事节奏，自动排出 4—6 个分镜的故事线——"
 "比如第一幕少女独立山间俯瞰云海，第二幕桃花林起舞，第三幕近景演唱，第四幕夕阳远景收尾。主角长什么样会被统一写进一份角色卡，"
 "之后哪个镜头要用到角色信息，都得回这张卡里取。全片主角形象能保持一致，靠的就是这一点：各环节共用同一份角色卡，跨镜头的外貌才不会跑偏。"),

("若用户上传了参考音乐，系统会在规划之前",
 "要是用户传了参考音乐，系统会在规划之前先把音频分析一遍：把每个节拍的时间点抠出来，认出全曲的节拍速度，再用聚类把整首歌切成主歌、副歌、间奏这些段落。"
 "这些段落的起止时间接着作为硬约束交给剧本智能体，逼着每个分镜的时长去贴音乐结构。这么一来，副歌自动配上情绪最浓的演唱镜头，主歌配叙事镜头，"
 "间奏留给空镜或转场，画面切换和音乐节奏也就对上了。"),

("尚不能直接驱动图像或视频模型",
 "剧本智能体给出的分镜脚本只有情节，还驱动不了图像或视频模型。接棒的是导演智能体（Director）。它把角色卡和分镜脚本读进去，给每个镜头写一段精确的画面描述"
 "——不是笼统一句“古装少女”就完事，得点明服饰颜色（藕荷色汉服、金色腰带）、镜头景别（广角远景还是中近景）、光线方向（侧逆光勾轮廓）、运镜方式（缓缓推进）。"
 "这一步是把大模型对故事的理解，翻成图像和视频模型能直接照做的视觉指令。"),

("其核心策略可概括为八个字",
 "画面描述备好，系统就进生成阶段。核心策略八个字：先出静态图，再生成视频。这么安排是因为图像模型在人物细节上比视频模型稳——"
 "先用图像把主角在这一镜的样子定下来，视频模型就只管处理时序动态，外貌跑偏的概率自然低了。所以每个分镜都分两步：先图像生成出一张参考图，"
 "再拿这张图当起始帧去驱动视频，最后产出 3—8 秒的动态片段。"),

("系统的解法称为末帧接力",
 "有了参考图，事还没完——几个镜头各生成各的，照样可能“换脸”，第一镜和第二镜的主角对不上。系统的办法叫末帧接力：一个镜头生成完，"
 "就把它的最后一帧抽出来，当下一镜头视频生成的起始参考。这样相邻镜头之间就有了直接的视觉衔接，主角的外貌、光线、背景色调都能顺着接下去，不会突然跳。"
 "另外系统里还有个镜头路由模块，会按镜头类型——演唱还是叙事——挑最合适的视频模型：演唱镜头优先用面部更稳的模型，叙事镜头则看画风在几个模型之间分。"),

("综合分一旦低于阈值便自动重试",
 "每个镜头一生成完，质检智能体（Verifier）立刻上，从视觉质量、跟角色卡的一致性、跟原始描述的吻合度三个角度分别打分。综合分一旦低于阈值就自动重试，"
 "最多三次：第一次微调生成参数，第二次换备用模型，第三次拿保底画面顶上，保证整条流水线不会被某个出问题的镜头卡住。这一整套不用人插手。"),

("所有分镜的视频按时间线拼接",
 "所有分镜的视频按时间线拼起来，混进音乐，加上歌词字幕，再按目标平台的要求编码导出。从用户那句“我要一个国风 MV”，到一份能直接发的视频文件，"
 "整个过程 8—12 分钟内自动跑完。"),

("这条链路的设计思路可归纳为四点",
 "这条链路的思路，说到底就几件事：用多智能体把复杂决策分给各管一摊的角色；用音乐分析把时间节奏灌进规划；靠先图后视频再加末帧接力，"
 "从两头去保跨镜头的一致性；最后用自动质检的闭环顶掉人工审片。"),

("其难点在于 LLM 本身无法感知音频",
 "先说思路。子问题 2 要的是分镜时长跟音乐段落对齐，难点在于 LLM 自己听不见音频——你不把段落时间戳明明白白告诉它，它切出来的镜头时长不是平均分就是瞎蒙，"
 "节奏感很差。所以这一节改走信号处理的路子：先从音频里把结构化的段落提出来，再以硬约束的形式塞进 Agent 的任务描述。"),

("音频先进行短时分帧",
 "先讲自相似矩阵和段落聚类。音频先做短时分帧（hop = 512，sr = 22050），再逐帧抽 13 维 MFCC 和 12 维 chroma——前者管音色，后者管音高分布，"
 "拼成 25 维特征向量，既能把主歌和副歌的音色差异分开，又留得住旋律走向。接着对全曲各帧两两算余弦相似度，搭出一张自相似矩阵；"
 "矩阵对角线附近那些块状的高相似区，对应的就是歌里的各个段落。系统拿“1 − 相似度”当距离，对帧序列做层次聚类（Ward linkage，让簇内平方差最小），"
 "切出若干段落区间，再按各段 RMS 能量从低到高贴标签：能量最低的归 intro/outro，最高的归 chorus，居中的归 verse 和 bridge。"),

("之所以采用 Ward linkage",
 "层次聚类这里用 Ward linkage，没用 single 或 average，是因为 MV 的段落边界对节奏切换挺敏感，而 Ward 倾向于切出大小比较均衡的簇，"
 "能躲开“一首歌被切成 1 长 5 短”那种极端。至于聚多少层、也就是切几段，交给轮廓系数（silhouette score）自己挑，范围卡在 [3, 8]："
 "少于 3 段叙事性不够；多于 8 段又容易把短间奏当成独立段落，把分镜时长切得太碎。"),

("再说明段落标签语义化所用的能量阈值",
 "再讲段落标签语义化用的能量阈值。这里不能简单按能量排个序硬切。光按位次定标签，碰上副歌前后能量差不多的歌（比如那种缓推的动力型电子乐），"
 "很容易把“两个 chorus”判成相邻。本系统改用 RMS 能量的相对阈值：低于全曲均值 0.7 倍的段落，算 intro/outro 的候选；高于均值 1.2 倍的，算 chorus 候选；"
 "其余算 verse；首尾两段则优先判 intro/outro，哪怕能量还没到阈值。这套启发式规则在 30 条测试集上的段落标签准确率到了 87%。"),

("兜底机制亦需说明",
 "还有兜底要交代。BPM 置信度低于 0.5 时，就不再施加节拍约束；段落数不正常（少于 2 段或多于 12 段）时，退回 8 秒等长切分；"
 "歌词转录是空的，就跳过歌词字幕；音频不到 10 秒，干脆不分析。每触发一档兜底，前端都会拿醒目提示告诉用户，免得静默降级，"
 "弄出“画面跟节拍对不上、用户还不知道为啥”的体验断层。"),
]


def set_text(p, text):
    if p.runs:
        p.runs[0].text = text
        for r in p.runs[1:]:
            r.text = ""
    else:
        p.add_run(text)


d = Document(PATH)
done = set()
for p in d.paragraphs:
    t = p.text
    for i, (snip, new) in enumerate(R):
        if i in done:
            continue
        if snip in t:
            set_text(p, new)
            done.add(i)
            print(f"[ok] #{i}: {snip[:22]}")
            break

missing = [R[i][0][:22] for i in range(len(R)) if i not in done]
if missing:
    print("[WARN] NOT matched:", missing)
else:
    d.save(PATH)
    print("saved", PATH, "| 共替换", len(done), "段")
